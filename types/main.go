package main

import "fmt"

func main() {
    var title string
    var copies int
    var name string
    title = "For the Love of Go"
    copies = 99
    name = "Tolkien"
    var edicion string
    edicion = "1 edición"
    var oferta bool
    var descuento int
    oferta = true
    descuento = 15

    fmt.Println(title)
    fmt.Println(copies)
    fmt.Println(name)
    fmt.Println(edicion)
    fmt.Println(oferta)
    fmt.Println(descuento)
}
